// 这个文件已废弃，不再被任何地方引用。
//
// 早期版本侧边栏文件浏览用的是「原地展开的树」（OutlineGroup + 这个 FileNode 节点类型），
// 现在改成了：点服务器行最右边的文件夹图标，整个侧边栏切换成该服务器「当前目录」的
// 平铺列表，点目录行钻进下一层、点顶部「返回」回退一层——不再需要树结构，所以也不再
// 需要这个节点类型。新设计见 ViewModels/FileBrowserStore.swift 和 Views/Sidebar/ServerListView.swift。
//
// 留着这个空文件只是因为工具权限不允许我直接删除磁盘上的文件；在 Finder 里
// 把它删掉是安全的，不影响编译（SwiftPM 不会因为多一个空文件报错）。
