// 这个文件已废弃，不再被任何地方引用。
//
// 远程文件浏览功能搬到了侧边栏——见 Views/Sidebar/ServerListView.swift 里的
// FileBrowserColumn / FileBrowserRow：点服务器行最右边的文件夹图标，整个侧边栏
// 切换成该服务器「当前目录」的平铺列表，点目录钻进下一层、点「返回」回退一层，
// 不再是底部一个独立的浏览器面板。
//
// 留着这个空文件只是因为工具权限不允许我直接删除磁盘上的文件；在 Finder 里
// 把它删掉是安全的，不影响编译（SwiftPM 不会因为多一个空文件报错）。
